using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Experimental.Rendering;

namespace Hbx.Assets
{
    public static class TexelUtils
    {
        /*public static FlipTexels(TextureParams texparams)
        {
            uint mipbsize = GraphicsFormatUtility.ComputeMipmapSize(texparams.width, texparams.height, texparams.format);
            uint blockbsize = GraphicsFormatUtility.GetBlockSize(texparams.format);
            uint blockwidth = GraphicsFormatUtility.GetBlockWidth(texparams.format);
            uint blockheight = GraphicsFormatUtility.GetBlockHeight(texparams.format);

            int blocksx = texparams.width / (int)blockwidth;
            int blocksy = texparams.height / (int)blockheight;

            byte[] flipped = new byte[texparams.texels.Length];



        }*/

        /*public static Vector2Int ComputeMipMapTexelSize(int width, int height, int level)
        {

        }*/
    }
}
